monitor.v4l2.rules = [
  {
    # Raw IPU7 capture nodes are consumed through libcamera, not directly.
    matches = [
      {
        api.v4l2.cap.driver = "isys"
      }
    ]
    actions = {
      update-props = {
        node.disabled = true
      }
    }
  }
]
